import React from "react"
import styles from "./job.module.sass"

const Job = ({ job, company }) => {
  const { id, name, location, created, closeDate } = job
  return <div></div>
}

export default Job
