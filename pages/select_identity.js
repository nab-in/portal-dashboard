import styles from "../styles/select.module.sass"

const select_identity = () => {
  return (
    <div className={styles.select}>
      <h2>Please select dashboard to view</h2>
    </div>
  )
}

export default select_identity
